# Multi-Source-Claim-Event-Resolver
Standalone GenLayer Intelligent Contract: Multi-Source Claim / Event Resolver  A reusable primitive that lets anyone create a natural-language claim (e.g. “Did X happen on date Y?”). Multiple evidence URLs can be submitted. The contract fetches the live content of each source, extracts structured answers with an LLM, and reaches consensus on
